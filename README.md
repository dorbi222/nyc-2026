# NYC 2026 – עדכון: בוסטון, Thursday ו־Sydney

## 1. להעלות את הקבצים
בריפו: Add file ← Upload files ← גוררים את כל הקבצים ← Commit changes.

## 2. לעדכן את חוקי האבטחה (חובה)
בלי השלב הזה בוסטון ו־Sydney לא יעבדו.
פיירבייס ← Firestore Database ← לשונית Rules ← מוחקים הכול, מדביקים את התוכן של `firestore.rules` ← Publish.

## 3. ליצור כניסה ל־Sydney
נכנסים לאתר בתור דור ← תפריט ← הגדרות ← "משתמשת נוספת: Sydney" ← מקלידים את הקוד ← "ליצור כניסה".

## 4. להיכנס פעם אחת בתור מוטי או בן
הכניסה הראשונה שלהם טוענת את הטיול לבוסטון ואת המשימות שלו.
עד שזה קורה, Sydney תראה את לשונית בוסטון ריקה.
